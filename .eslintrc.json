{
  "extends": [
    "next/core-web-vitals",  // Use the core web vitals configuration for Next.js
    "next/typescript",       // Extend the TypeScript configuration for Next.js
    "next"                   // Use the base Next.js configuration
  ],
  "rules": {
    "@next/next/no-img-element": "off"  // Disable the no-img-element rule
  }
}
